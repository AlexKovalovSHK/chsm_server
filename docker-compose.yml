version: "3.9"

services:
  app:
    build: .
    image: chsm_serv:latest
    container_name: chsm_serv
    restart: unless-stopped
    ports:
      - "5008:5008"
    env_file:
      - .env
    environment:
      - PORT=5008
      - NODE_ENV=production